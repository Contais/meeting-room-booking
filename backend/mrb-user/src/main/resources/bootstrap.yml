spring:
  application:
    name: mrb-user
  cloud:
    nacos:
      server-addr: ${NACOS_SERVER_ADDR:localhost:8848}
      config:
        file-extension: yml
        namespace: dev
      discovery:
        namespace: dev
