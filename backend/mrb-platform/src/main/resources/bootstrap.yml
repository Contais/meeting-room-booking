spring:
  application:
    name: mrb-platform
  cloud:
    nacos:
      server-addr: localhost:8848
      config:
        file-extension: yml
        namespace: dev
      discovery:
        namespace: dev
